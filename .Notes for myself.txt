    public class Grid<TGridObject>
    {
        public event EventHandler<OnGridObjectChangedEventArgs> OnGridObjectChanged;
        public class OnGridObjectChangedEventArgs : OnGridObjectChangedEventArgsArgs

if (Input.GetKeyDown(KeyCode.R))
        {
            SetSailSpeeds(-1);
        }

cannons
particle effects for water trail
ask ash about fog to hide the horizon
add the "R" function back to the ship controls (E, R)
ocean follows the player


sequel Child of the Sea ;)